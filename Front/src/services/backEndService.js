/**
 * A class to format backend data for user-related information.
 */
class BackendDataFormatter {
  formatUserMainData(data) {
    return data.data;
  }

  formatUserActivity(data) {
    return data.data;
  }

  formatUserAverageSessions(data) {
    return data.data;
  }

  formatUserPerformance(data) {
    return data.data;
  }
}

export default BackendDataFormatter;
